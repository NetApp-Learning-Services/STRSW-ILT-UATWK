apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: c2-svmdst-nfs-dv-vmdk-snap-1
  namespace: testvm3
spec:
  volumeSnapshotClassName: csi-snap-class
  source:
    persistentVolumeClaimName: dv-vmdk